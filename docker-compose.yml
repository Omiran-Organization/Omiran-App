version: '3'

services:
    backend:
        build: ./backend
        volumes:
            - ./backend:/go/src/app
        ports:
            - 80:8080 
    frontend:
        build: ./frontend
        volumes:
            - ./frontend:/app
        ports:
            - 3000:3000
        depends_on: 
            - backend
            